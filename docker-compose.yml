version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - telegram_api_key=123:ABCDE
      - authorized_telegram_chat_id=1234
      - debug=False